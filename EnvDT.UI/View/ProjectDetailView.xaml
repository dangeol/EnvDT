<UserControl x:Class="EnvDT.UI.View.ProjectDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:EnvDT.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition />
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Label Grid.Row="1" Content="ProjectNumber" Margin="10 0"/>
    <TextBox Grid.Row="2" Text="{Binding Project.ProjectNumber,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10"/>

    <Label Grid.Row="3"  Content="ProjectClient" Margin="10 0"/>
    <TextBox Grid.Row="4" Text="{Binding Project.ProjectClient,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10"/>

    <Label Grid.Row="5"  Content="ProjectName" Margin="10 0"/>
    <TextBox Grid.Row="6" Text="{Binding Project.ProjectName,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10"/>

    <Label Grid.Row="7"  Content="ProjectAddress" Margin="10 0"/>
    <TextBox Grid.Row="8" Text="{Binding Project.ProjectAddress,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10"/>

    <DockPanel Grid.Row="9" Margin="10">
      <StackPanel DockPanel.Dock="Right">
        <Button Content="Load" Width="125"  Margin="10"
                Command="{Binding OpenLabReportCommand}"/>
        <Button Content="Remove" Width="125"  Margin="10"
                Command="{Binding RemoveSampleCommand}"/>
      </StackPanel>
      <DataGrid ItemsSource="{Binding LabReports}" 
                SelectedItem="{Binding SelectedLabReport,Mode=TwoWay}"
                AutoGenerateColumns="False" RowHeaderWidth="0">
        <DataGrid.Columns>
          <DataGridTextColumn Header="LabReports" Width="*"
             Binding="{Binding LabReport,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        </DataGrid.Columns>
      </DataGrid>
    </DockPanel>

    <StackPanel Grid.Row="10" Orientation="Horizontal">
      <Button Content="Save" Width="125" Margin="10"
              Command="{Binding SaveCommand}"/>

      <Button Content="Delete" Width="125" Margin="10"
              Command="{Binding DeleteCommand}"/>
    </StackPanel>    
    
  </Grid>
</UserControl>
