<UserControl x:Class="EnvDT.UI.View.LabDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resx="clr-namespace:EnvDT.UI.Properties"
             xmlns:loc="clr-namespace:EnvDT.UI.Settings.Localization"
             loc:Translation.ResourceManager="{x:Static resx:Strings.ResourceManager}"
             xmlns:view="clr-namespace:EnvDT.UI.View"
             xmlns:viewModel="clr-namespace:EnvDT.UI.ViewModel"            
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="430" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition />
    </Grid.RowDefinitions>

    <Label Grid.Row="1" Content="{loc:Loc LabDetailView_Label_LabCompany}" Margin="10 0"/>
    <TextBox Grid.Row="2" Text="{Binding Laboratory.LabCompany,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="400" Margin="20 0 10 5"/>

    <Label Grid.Row="3" Content="{loc:Loc LabDetailView_Label_LabName}" Margin="10 0"/>
    <TextBox Grid.Row="4" Text="{Binding Laboratory.LabName,
             Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="400" Margin="20 0 10 5"/>

    <Label Grid.Row="1" Grid.Column="1" Content="{loc:Loc LabDetailView_Label_Country}" Margin="10 0"/>
    <ComboBox Grid.Row="2" Grid.Column="1" Width="200" Margin="20 0 10 5" HorizontalAlignment="Left"
              SelectedValue="{Binding Path=Laboratory.CountryId,UpdateSourceTrigger=PropertyChanged}"
              SelectedValuePath="LookupItemId"
              ItemsSource="{Binding Path=Laboratory.Countries}"
              DisplayMemberPath="DisplayMember">
    </ComboBox>

    <StackPanel Grid.Row="5" Orientation="Horizontal">
      <Button Content="{loc:Loc UI_Button_Save}" Margin="20 10"
              Command="{Binding SaveCommand}"/>

      <Button Content="{loc:Loc UI_Button_Delete}" Margin="10 10"
              Command="{Binding DeleteCommand}"/>
    </StackPanel>

    <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal">
      <DockPanel IsEnabled="{Binding IsConfigXlsxDetailViewEnabled}">
        <view:ConfigXlsxDetailView DataContext="{Binding ConfigXlsxDetailViewModel}" />
      </DockPanel>
      <DockPanel IsEnabled="{Binding IsConfigXmlDetailViewEnabled}">
        <view:ConfigXmlDetailView DataContext="{Binding ConfigXmlDetailViewModel}" />
      </DockPanel>
    </StackPanel>
  </Grid>

</UserControl>
